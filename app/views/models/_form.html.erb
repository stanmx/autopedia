<%= form_for(@model) do |f| %>
  <% if @model.errors.any? %>
    <div id="error_explanation" class="alert alert-block">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <h4><%= pluralize(@model.errors.count, "error") %> no permite que este Modelo sea creado:</h4>
      <br />
      <ul>
      <% @model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label 'Marca' %>
    <%= f.collection_select :brand_id, Brand.all, :id, :title, { :prompt => true } %>
  </div>
  <div class="field">
    <%= f.label 'Titulo' %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label 'Descripción' %>
    <%= f.text_area :description, :rows => 6 %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
