<%= form_for(@state) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation" class="alert alert-block">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <h4><%= pluralize(@state.errors.count, "error") %> no permiten que este Estado sea creado:</h4>
      <br />
      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Pais' %>
    <%= f.collection_select :country_id, Country.all, :id, :title, { :prompt => true } %>
  </div>
  <div class="field">
    <%= f.label 'Titulo' %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label 'Descripción' %>
    <%= f.text_area :description, :rows => 6 %>
  </div>
  <div class="actions"> 
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
